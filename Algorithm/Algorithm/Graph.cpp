#include "Graph.hpp"
#include <iostream>
#include <map>
#include <tuple>
#include <vector>

using namespace std;

Graph::Graph(int size) { Graph::graphSize = size; }

void Graph::addNode(int node) {}
